# 大肥鱼桌宠 · DSwhale Desktop Pet

一只住在桌面上的 Live2D 桌宠：**大肥鱼**（模型《DeepSeek 鲸鱼娘》）。

她会跟你聊天、记住你说过的事、慢慢形成对你的印象；你玩游戏看视频时她随口评论两句，
会陪你读书、给你放歌；也会累、会睡着，你回来找她又会醒。
平时她会自己找点事做——挤番茄酱、吹泡泡、自拍——身上的装扮也能随你换。

> 本仓库**只包含大肥鱼这一只**，且**包含她的 Live2D 模型素材**（已获原作者许可，见下「许可」）。

---

## 她会做什么

| 功能 | 说明 |
|---|---|
| 💬 对话 | 接入任意 OpenAI 兼容接口（DeepSeek / Ollama / 其他），有独立人设：偏可爱、傲娇、喜欢偷吃大白饭 |
| 🧠 记忆 | 聊天里提到的偏好与经历会存成记忆，形成「性格画像」与「关系形象」，下次主动提起 |
| 🎤 语音输入 | 本地 sherpa-onnx 识别，喊名字（前 2~3 字）唤醒，8 秒内免唤醒 |
| 🔊 语音输出 | Edge TTS 或火山引擎声音复刻（默认关闭，需自配 Key / 音色） |
| 🎮 陪玩 | 隔一段时间看一眼你的屏幕、随口评论（需另配「视觉模型」；画面没变就不吭声、不浪费调用） |
| 📖 读书 | 框选屏幕区域，OCR 读出你在读什么，陪你读并评论 |
| 🎵 音乐 | 本地曲库播放，支持语音点歌与「说完再播」 |
| 🎭 形象 | 8 段模型动作（挤番茄酱 / 吹泡泡 / 喷水 / 开盖 / 自拍 / 快速自拍…）、8 个装扮槽、8 档情绪各带表情池 |

## 直接下载成品（不想碰源码）

百度网盘：<https://pan.baidu.com/s/1SMvJiDkVZhNoARs75eDoZA>　提取码：`sgqg`

解压后就是一个自包含的 exe（`DafeiyuPet.exe`，约 600MB）：**不用装 Python、不用装别的东西**。
双击后要等十几秒界面才出来 —— 它正在把内容解压到临时目录，不是卡死。
第一次用记得先配 API Key（见下面第 2 步），不然她不会回话。

> 想自己改代码、自己打包，或者只是不想下 600MB？看下面「从源码运行」或 [`SETUP.md`](SETUP.md)。

## 快速上手（从源码运行）

1. **Windows x64 + Python 3.10**（live2d-py 的 Cubism native 是 cp310 扩展，3.11/3.12 装不上）
2. 装依赖：
   ```bat
   python -m pip install -r requirements.txt
   ```
3. 下模型（语音识别用，约 244MB，带 SHA256 校验；不下只是"语音识别"用不了）：
   ```bat
   python tools/fetch_models.py
   ```
   想要记忆向量召回更准，再加 `--with-bge`（多 92MB）。
4. 双击 **`run.bat`**（或 `python Dafeiyu.py`）。首次启动要解压/加载模型，等十几秒。
5. **右键点她 → ⚙️ 设置 → API 密钥 → 文本模型设置…**，选提供方预设、填你自己的 Key，保存。
   不填这一步她不会回话。

## 操作

| 操作 | 效果 |
|---|---|
| 左键点她 | 触摸反应（随机播一段触摸动作）；陪玩模式下等于手动让她看一眼屏幕 |
| 按住拖动 | 把她挪到桌面任意位置 |
| 右键点她 | 主菜单：语音开关 / 切换 TTS / 陪玩 / 读书 / 音乐 / 🎩 装扮 / 设置 |
| 输入框回车 | 打字聊天（输入框可以隐藏，右上角菜单里可再打开） |
| 喊名字 | 开启语音后喊「大肥鱼」（前 2~3 字即可），8 秒内可直接说话 |
| 🎩 装扮 | 眼镜 / 贴纸 / 发饰 / 桌布 / 魔爪 / 桌面摆设 / 桌面显示（可多选）/ 手持道具 |

## 数据放在哪

运行时数据都在 `userdata/`（**不会**上传到任何地方）：

| 文件 | 内容 |
|---|---|
| `config.json` | 所有设置：模型与密钥、TTS、话题定时器、陪玩/读书、缩放、输入框状态 |
| `memory.db` / `memories.json` | 记忆库（话题、事件、性格画像、关系形象） |
| `chats.json` | 聊天记录 |
| `dafeiyu.log` | 运行日志——出问题先看它 |

## 许可与素材

- **代码与文档**：MIT（见 [`LICENSE`](LICENSE)）。
- **Live2D 模型素材**（`live2d_viewer/`）：模型制作 **B站 @氵六青**（uid 11272072）。
  已获作者许可使用与二次传播；作者原《使用须知》（见 [`使用须知.txt`](使用须知.txt)）：
  **商用直播 √、自印物料 √，禁止任何形式的盗用与出售，此模型为无偿分享**。
  本仓库对该素材的**再分发**采用 **CC BY-NC-SA 4.0**（署名—非商业性使用—相同方式共享），
  完整条款与署名见 [`LICENSE-Live2D.txt`](LICENSE-Live2D.txt)。**转载/二次创作请署名并保留本许可。**
- **语音 / 向量模型**（`models/`，不随仓库分发，用 `tools/fetch_models.py` 下载）：
  sherpa-onnx Paraformer `Apache-2.0`、silero VAD `MIT`、BAAI bge-small-zh `MIT`。

## 致谢

- 模型：**B站 @氵六青**（uid 11272072）——《DeepSeek 鲸鱼娘》
- Live2D Python 绑定：[live2d-py](https://github.com/Arkueid/live2d-py)（MIT）
- 语音识别：[sherpa-onnx](https://github.com/k2-fsa/sherpa-onnx)（Apache-2.0）
- 记忆向量：[BAAI/bge-small-zh-v1.5](https://huggingface.co/BAAI/bge-small-zh-v1.5)
